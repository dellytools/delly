As an end user, simply `pip install` with `python2.7.txt`.
You probably want to create an environment with `virtualenv` first.

For example...
```bash
$ cd suave
$ virtualenv venv
$ source venv/bin/activate
$ pip install -r requirements/python2.7.txt
``` 
